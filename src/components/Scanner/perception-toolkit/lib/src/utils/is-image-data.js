/**
 * Convenience function used internally for detecting `ImageData` vs
 * `HTMLImageElement`.
 */
export function isImageData(imgData) {
    return typeof imgData.data !== 'undefined';
}

//# sourceMappingURL=is-image-data.js.map
